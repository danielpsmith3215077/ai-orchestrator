import ScrollReveal from "./ScrollReveal";

/** @deprecated Prefer ScrollReveal — kept for incremental migration. */
export default function FadeInSection(props) {
  return <ScrollReveal {...props} />;
}
